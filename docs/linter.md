# Linter
We use Pint, ESLint, and Stylelint to apply automatic code style fixes. You can easily run it using this command:

```shell
$ docker compose exec app composer lint
# This runs Pint, ESLint, Stylelint, and DB Graph generator
```

**Always run these commands before committing.**
